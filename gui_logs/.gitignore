# Ignore all log files but allow documentation and configuration
*.log
*.jsonl

# Ignore structured log directory contents but keep the directory
structured/
!structured/.gitkeep

# Allow configuration and documentation files
!README.md
!*.md
!log_config.json
!*.json

# Keep any analysis or summary files
!*summary*
!*analysis*